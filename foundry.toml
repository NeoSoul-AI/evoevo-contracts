[profile.default]
src     = "src"
out     = "out"
libs    = ["lib"]
test    = "test"
script  = "script"
solc    = "0.8.24"
optimizer       = true
optimizer_runs  = 200
via_ir          = true
evm_version     = "cancun"

[profile.ci]
fuzz = { runs = 1000 }

[rpc_endpoints]
localhost    = "http://localhost:8545"
sepolia      = "${SEPOLIA_RPC_URL}"
base_sepolia = "${BASE_SEPOLIA_RPC_URL}"
bsc          = "${BSC_RPC_URL}"
bsc_testnet  = "${BSC_TESTNET_RPC_URL}"
zero_g_testnet = "${ZERO_G_TESTNET_EVM_RPC_URL}"
zero_g_mainnet = "${ZERO_G_MAINNET_EVM_RPC_URL}"

[etherscan]
sepolia      = { key = "${ETHERSCAN_API_KEY}", url = "https://api-sepolia.etherscan.io/api" }
base_sepolia = { key = "${BASESCAN_API_KEY}",  url = "https://api-sepolia.basescan.org/api" }
bsc          = { key = "${BSCSCAN_API_KEY}",   url = "https://api.bscscan.com/api" }
bsc_testnet  = { key = "${BSCSCAN_API_KEY}",   url = "https://api-testnet.bscscan.com/api" }
